<div>
    <h1>Profile</h1>
    <p>Username: {{data.passport.user.name}}</p>
    <p>Email: {{data.passport.user.email}}</p>

    <form action="/api/users/logout" method="GET">
    <button type="submit">Logout</button>
    </form>
</div>